/**app.wxss**/
/*隐藏滚轮*/

::-webkit-scrollbar {
  /* display: none; */
  height: 0;
  width: 0;
  color: transparent;
}

/* input 样式*/
.input-placeholder {
  color: #ccc;
}

page {
  height: 100%;
  width: 750rpx;
  position: relative;
  font-size: 28rpx;
}

.container {
  width: 100%;
  height: 100%;
  position: relative;
  /* display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: space-between;
    padding: 200rpx 0;
    box-sizing: border-box; */
}


button::after {
  border: none;
}

button {
  border-radius:0;
}